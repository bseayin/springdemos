server:
  port: 9001

spring:
  application:
    name: Server-Consumer

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
